# Contacts App — UC‑02: User Authentication
This branch extends UC‑01 by adding a basic authentication system using simple polymorphism. The user can log in using either username/password or a simulated OAuth email flow.
