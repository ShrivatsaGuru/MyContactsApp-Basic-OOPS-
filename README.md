# Contacts App — UC‑11: Contact Type Tagging (Person / Organization)
This branch improves the contact creation process by assigning a default type tag to each new contact. Users must choose whether a contact represents a person or an organization.
